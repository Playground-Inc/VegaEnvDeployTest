---
Resources: # https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-dynamodb-table.html
  #
  # DynamoDB: AUTH_CODES
  #
  DynamoAuthCodesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: AUTH_CODES${self:custom.dynamoSfx}
      AttributeDefinitions:
        - AttributeName: attr
          AttributeType: S
      BillingMode: PAY_PER_REQUEST
      KeySchema:
        - AttributeName: attr
          KeyType: HASH
      SSESpecification:
        SSEEnabled: true
      TableClass: STANDARD
      Tags:
        - Key: TABLE
          Value: AUTH_CODES
        - Key: STAGE
          Value: ${self:custom.generalStage}
      TimeToLiveSpecification:
        AttributeName: expires
        Enabled: true
  #
  # DynamoDB: CHILDS
  #
  DynamoChildsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: CHILDS${self:custom.dynamoSfx}
      AttributeDefinitions:
        - AttributeName: id_child
          AttributeType: S
        - AttributeName: app
          AttributeType: S
        - AttributeName: created
          AttributeType: N
      BillingMode: PAY_PER_REQUEST
      GlobalSecondaryIndexes:
        - IndexName: BY_CREATED_DATE
          KeySchema:
            - AttributeName: app
              KeyType: HASH
            - AttributeName: created
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
      KeySchema:
        - AttributeName: id_child
          KeyType: HASH
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      SSESpecification:
        SSEEnabled: true
      TableClass: STANDARD # or STANDARD_INFREQUENT_ACCESS
      Tags:
        - Key: TABLE
          Value: CHILDS
        - Key: STAGE
          Value: ${self:custom.generalStage}
